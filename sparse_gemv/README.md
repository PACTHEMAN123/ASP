# GEMV

a custom gemv for speed up sparse GEMV.

## Installation

to install this operator and use as a module (pytorch should be already installed in the virtual environment):

```bash
uv pip install -e . --no-build-isolation
```

## Test

```bash
uv run gemv_test.py
```